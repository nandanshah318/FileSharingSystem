const fileUploadController = (req,res) => {
    try {
        console.log("fileUploadWorking");
        return res.status
    } catch (error) {
        console.error(error);
    }
};

module.exports = {fileUploadController};